---
- name: Install Flux OS
  ansible.builtin.include_tasks:
    file: fluxos-install.yml
    apply:
      tags:
        - fluxos-install
        - fluxos
      become: true
  tags:
    - fluxos-install
    - fluxos
    - never

- name: Remove Flux OS
  ansible.builtin.include_tasks:
    file: fluxos-purge.yml
    apply:
      tags:
        - fluxos-purge
        - fluxos
      become: true
  tags:
    - fluxos-purge
    - fluxos
    - never
