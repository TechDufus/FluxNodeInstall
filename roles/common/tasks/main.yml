---
# tasks file for common
- name: Gather WAN IP
  ansible.builtin.include_tasks:
    file: get-ip.yml
    apply:
      tags:
        - get-ip
        - common
      become: true
  tags:
    - get-ip
    - common
    - never
